<h3 class="subheader">Edit Pattern</h3>
<hr />
<%= form_for @pattern do |f| %>

  <%= render partial: 'form', locals: {f: f} %>
  
  <%= f.submit "Update", class: 'small blue nice button radius' %>
  <%= link_to "Cancel", patterns_path, class: 'small red nice button radius' %>
<% end %>