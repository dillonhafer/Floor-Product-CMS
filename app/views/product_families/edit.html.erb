<h3 class="subheader">Edit Product Family</h3>
<hr />
<%= form_for @product_family do |f| %>

  <%= render partial: 'form', locals: {f: f} %>
  
  <%= f.submit "Update", class: 'nice button radius' %>
<% end %>