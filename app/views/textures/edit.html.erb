<h3 class="subheader">Edit Texture</h3>
<hr />
<%= form_for @texture do |f| %>

  <%= render partial: 'form', locals: {f: f} %>
  
  <%= f.submit "Update", class: 'small blue nice button radius' %>
  <%= link_to "Cancel", textures_path, class: 'small red nice button radius' %>
<% end %>