<h3 class="subheader">Textures</h3>

<hr />

<%= link_to_icon 'plus-sign icon-white', 'New Texture', new_texture_path, class: 'small green button nice radius' %>

<table>
  <thead>
    <tr>
      <th>Name</th><th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @textures.all.each do |pc| %>
    <tr>
      <td><%= pc.name %></td><td><%= link_to_icon 'edit icon-white', 'Edit', edit_texture_path(pc), class: 'small blue button radius' %> <%= link_to_icon 'trash icon-white', 'Delete', texture_path(pc), method: :delete, data: {confirm: '1'}, class: 'small red button nice radius' %></td>
    </tr>
    <% end %>
  </tbody>
</table>
