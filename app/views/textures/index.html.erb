<h3 class="subheader">Textures</h3>

<hr />

<%= link_to 'New Texture', new_texture_path, class: 'blue button nice radius' %>

<table>
  <thead>
    <tr>
      <th>Name</th><th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @textures.all.each do |pc| %>
    <tr>
      <td><%= pc.name %></td><td><%= link_to 'edit', edit_texture_path(pc), class: 'small yellow button radius' %> <%= link_to 'delete', texture_path(pc), method: :delete, data: {confirm: '1'}, class: 'small red button nice radius' %></td>
    </tr>
    <% end %>
  </tbody>
</table>
